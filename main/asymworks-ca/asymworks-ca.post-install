#!/bin/sh
mkdir -p /usr/local/share/ca-certificates/asymworks
find /usr/share/ca-certificates/asymworks -type f -exec ln -s {} /usr/local/share/ca-certificates/asymworks/ \;
/usr/sbin/update-ca-certificates